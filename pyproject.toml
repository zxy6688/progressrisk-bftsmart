[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "infocom-pbft-progress-v1"
version = "0.1.0"
description = "Controlled PBFT progress-outcome simulator and Bayesian shard-risk controller"
requires-python = ">=3.10"
